# Discord credentials — see docs/discord-setup.md
DISCORD_APP_ID=
DISCORD_BOT_TOKEN=
DISCORD_USER_TOKEN=

# Anthropic auth is reused from your existing Claude Code login — no key needed here.
# (Sidecar shells out to the `claude` CLI on your PATH.)
